<template>
  <div class="card shadow-lg product-card" style="width: 18rem">
    <img
      :src="`/images/food-image/${this.product.gambar}`"
      class="card-img-top"
      alt="..."
    />
    <div class="card-body">
      <h5 class="card-title">{{ product.nama }}</h5>
      <p class="card-text">Harga : Rp. {{ product.harga }}</p>
      <router-link
        class="btn btn-lg btn-success"
        :to="`/food/${this.product.id}`"
      >
        <b-icon-cart />
        Pesan
      </router-link>
    </div>
  </div>
</template>

<script>
export default {
  name: "ProductCard",
  props: {
    product: {
      type: Object,
      required: true,
    },
  },
};
</script>
